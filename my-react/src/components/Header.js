import React from "react";

const Header = (props) => (
    <nav>
        <div className="nav-wrapper">
            <a href="#" className="brand-logo center">BIT</a>
        </div>
    </nav>
)

export default Header;